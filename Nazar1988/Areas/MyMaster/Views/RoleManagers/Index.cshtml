@model ReflectionIT.Mvc.Paging.PagingList<Nazar1988.Models.ViewModels.RolesViewModel>
@using ReflectionIT.Mvc.Paging
@addTagHelper*,ReflectionIT.Mvc.Paging
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_MymasterLayout.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header bg-light">
                نقش ها
            </div>
            <div class="card-body">
                <p>
                    <a asp-action="AddRole" class="btn btn-primary">افزودن نقش جدید</a>
                </p>
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>
                                @Html.SortableHeaderFor(model => model.RoleName)
                            </th>
                            <th class="text-center">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.RoleName)
                                </td>
                                <td class="text-center">
                                    <a asp-action="EditRole" asp-route-id="@item.RoleID" class="btn btn-success btn-icon">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                    <a asp-action="DeleteRole" asp-route-id="@item.RoleID" class="btn btn-danger btn-icon">
                                        <i class="fa fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <nav>
                    @await this.Component.InvokeAsync("Pager", new { PagingList = this.Model })
                </nav>
            </div>
        </div>
    </div>
</div>
